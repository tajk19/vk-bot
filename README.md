# VK Bot - Готовая к запуску версия

Это полностью рабочая версия бота для записи на стирку. Просто настройте и запускайте!

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
cd /home/ramaz/Documents/projects/vk-bot-ready
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройте Google Sheets

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект и включите **Google Sheets API** и **Google Drive API**
3. Создайте Service Account и скачайте ключ JSON
4. Сохраните скачанный файл как `credentials.json` в этой папке
5. Создайте Google Sheets таблицу с тремя листами:
   - **List** - для записей
   - **Blacklist** - для черного списка
   - **Schedule** - для расписания

#### Структура листа Schedule:

| День недели | Начало | Конец |
|-------------|--------|-------|
| понедельник | 8      | 22    |
| вторник     | 8      | 22    |
| среда       | 8      | 22    |
| четверг     | 8      | 22    |
| пятница     | 8      | 22    |
| суббота     | 9      | 20    |
| воскресенье | 9      | 20    |

6. Добавьте email из `credentials.json` (поле `client_email`) в соавторы таблицы с правами редактора

### 3. Настройте VK бота

1. Создайте сообщество VK
2. Получите токен доступа:
   - "Управление" → "Работа с API" → "Создать ключ"
   - Права: "Сообщения сообщества", "Управление сообществом"
3. Включите Long Poll:
   - "Настройки" → "Работа с API" → "Long Poll API"
   - Включите и выберите события: "Входящие сообщения"

### 4. Создайте файл .env

Скопируйте пример и заполните своими данными:

```bash
cp .env.example .env
nano .env
```

Заполните:
- `VK_TOKEN` - токен VK сообщества
- `SPREADSHEET_NAME` - название Google Sheets таблицы
- `ADMIN_IDS` - ваш VK ID (можно несколько через запятую)

### 5. Запустите бота

```bash
python bot.py
```

Готово! Бот работает ✅

## 📋 Команды

### Для пользователей:
- **"Записаться"** - записаться на стирку
- **"Мои записи"** - посмотреть свои записи
- **"Отменить запись"** - отменить запись
- **"Связаться с админом"** - получить контакт администратора

### Для администраторов:
- **"Неподтвержденные"** - список заявок
- **"Список записей"** - все подтвержденные записи
- **"Блокировать слот"** - заблокировать время
- **"Разблокировать слот"** - разблокировать
- **"Черный список"** - показать ЧС
- **"+ в черный список"** - добавить в ЧС
- **"- из черного списка"** - удалить из ЧС

## ⚙️ Настройка (опционально)

В файле `.env` можно изменить:

```env
# Максимум слотов на день для одного пользователя
MAX_SLOTS_PER_DAY=3

# Интервал между слотами (минуты)
SLOT_INTERVAL_MIN=30

# Длительность стирки (минуты)
WASH_DURATION_MIN=60

# Уведомление до стирки (минуты)
NOTIFY_BEFORE_MIN=10

# Проверка завершения после стирки (минуты)
NOTIFY_AFTER_MIN=60

# Время жизни кеша (секунды)
CACHE_TTL=300
```

## 🔒 Безопасность

⚠️ **НЕ коммитьте эти файлы в Git:**
- `.env` - содержит токены
- `credentials.json` - содержит ключи Google API

Они уже добавлены в `.gitignore`.

## 📁 Структура проекта

```
vk-bot-ready/
├── bot.py              # Главный файл
├── config.py           # Конфигурация
├── cache.py            # Кеширование
├── google_sheets.py    # Работа с Google Sheets
├── keyboards.py        # Клавиатуры VK
├── notifications.py    # Уведомления
├── handlers/
│   ├── user.py         # Обработчики для пользователей
│   ├── admin.py        # Обработчики для админов
│   └── role.py         # Базовый класс
├── requirements.txt    # Зависимости Python
├── .env               # Ваша конфигурация (создайте из .env.example)
├── credentials.json   # Ключи Google (создайте из credentials.json.example)
└── README.md          # Этот файл
```

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что Long Poll включен в VK
- Убедитесь, что токен правильный в `.env`
- Проверьте, что бот запущен: `python bot.py`

### Ошибки Google Sheets
- Проверьте, что `credentials.json` существует
- Убедитесь, что Service Account добавлен в соавторы таблицы
- Проверьте название таблицы в `.env`

### "VK_TOKEN не задан"
- Создайте файл `.env` из `.env.example`
- Заполните все обязательные поля

## 💡 Полезные ссылки

- Ваша Google Sheets таблица: (добавьте ссылку)
- Ваше VK сообщество: (добавьте ссылку)
- Google Cloud Console: https://console.cloud.google.com/

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что все файлы на месте
3. Проверьте права доступа к Google Sheets

---

**Версия**: 1.0 (стабильная)
**Дата**: 2026-01-06
